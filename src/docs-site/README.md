# ChildSafeNet Docs Site (Docusaurus)

## Local run
```bash
cd docs-site
npm install
npm run start
```

## Build
```bash
npm run build
```

## Deploy (GitHub Pages)
- Ensure GitHub Pages source is `gh-pages` branch
- Run the workflow `docs-site` (or `npm run deploy`)
